import { DowpdownDirective } from './dowpdown.directive';

describe('DowpdownDirective', () => {
  it('should create an instance', () => {
    const directive = new DowpdownDirective();
    expect(directive).toBeTruthy();
  });
});
